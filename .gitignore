/bin/
.DS_Store
*.test

# Local runtime state. Desired state and non-secret evidence may be committed;
# credentials and caches must not.
.ansible/
.runtime/
secrets/

# Appliance bytes, scan roots, and qualification cache are runtime state.
generated/artifacts/
generated/runtime/
*.tar.zst
*.qcow2
.trivy/

# Legacy OpenTofu state and provider caches may remain in existing checkouts.
.terraform/
*.tfstate
*.tfstate.*
*.tfplan
