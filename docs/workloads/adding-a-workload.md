# Adding a workload

Add a module declaration to `site.yml` with its zone, fixed address, aliases, SSH policy, mTLS, monitoring, and backup requirements. The canonical model must then drive infrastructure, firewall, DNS, PKI, Zabbix, backup, verification, portal, and SSH projections without duplicate documentation edits.
