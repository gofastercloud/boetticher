# Dedicated data disk

The OS disk contains Proxmox root. The data disk contains `vg_homelab` with guest thin storage, backup filesystem, and reserved capacity. Guest databases remain ordinary guest disks.
