# Storage recovery

Same-disk backups are not disaster recovery. Preserve the private site repository, Age recovery copy, and required Proxmox backup material independently when recovery from disk loss matters.
