# Cloudflare extension pattern

Cloudflare Tunnel and Cloudflare One private-network access have different exposure and routing models. V1 provides recipes only for publishing a selected protected application or configuring private access; neither is part of the trusted core.
