# Cloudflare extension pattern

Cloudflare Tunnel and Cloudflare One private-network access have different
exposure and routing models. Boetticher 0.4 does not include either as a
supported install path. This page is a design note for a future extension,
not a promise that Boetticher configures Cloudflare.
