# Troubleshooting

Start with `homelab access`, then `homelab doctor`, then `homelab verify`. A `NOT TESTED`, `HOLD`, or `INCONCLUSIVE` result must remain visible until the corresponding evidence exists. Check the model revision before investigating a generated projection.
