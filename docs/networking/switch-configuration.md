# Switch configuration

The completed physical design uses an 802.1Q trunk between the second Proxmox NIC and a managed switch. Configure access ports for the intended zone and enable client/port isolation for physical SANDBOX peers where supported. OPNsense remains the routing and firewall boundary.
