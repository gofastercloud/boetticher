# DHCP, DNS, and NTP

TRUSTED, SERVERS, and MGMT receive both `10.10.20.10` and `10.10.20.11` for DNS/NTP. SANDBOX receives only `10.10.50.1`. MGMT has reservations only and no unknown-client pool. `lab-dns-01` and `lab-dns-02` converge independently.
