# SANDBOX

SANDBOX uses VLAN 50 and `10.10.50.0/24` in V1. Clients use OPNsense for DNS, NTP, and gateway. SANDBOX cannot reach TRUSTED, SERVERS, or MGMT through the routed policy. The deferred `/32` DHCP experiment is not an isolation claim.
