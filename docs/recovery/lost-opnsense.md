# Lost OPNsense

Recreate the firewall VM from the pinned image/install path, restore WAN and VLAN interfaces, establish MGMT reachability, and apply the API-driven policy. OPNsense is the security boundary; guest convergence alone is not a substitute.
