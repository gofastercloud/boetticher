# Lost Proxmox

Install the supported Proxmox release with node name `lab-proxmox-01`, restore the HOME-side DHCP path, recover the site repository and Age identity, then run `preflight`, `bootstrap-endpoint set`, and the qualified bootstrap sequence. The normal internal recovery path is still through the Proxmox bastion. Do not claim recovery until the deployed model revision, SSH host key, authenticated journeys, and negative security tests are reverified.
