# Lost DNS

Rebuild the failed DNS node from its independent Ansible convergence. Keep both DNS/NTP addresses configured for clients and verify each node independently before treating service redundancy as restored.
