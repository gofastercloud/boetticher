---
- name: Prepare the enrolled Proxmox host
  hosts: proxmox
  connection: ssh
  become: false
  gather_facts: true

  vars:
    ansible_python_interpreter: /usr/bin/python3

  roles:
    - proxmox-baseline
