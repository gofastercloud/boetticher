---
- name: Enable and start required Proxmox services
  ansible.builtin.systemd_service:
    name: "{{ item }}"
    enabled: true
    state: started
  loop:
    - pve-cluster.service
    - pvedaemon.service
    - pvestatd.service
    - pveproxy.service
