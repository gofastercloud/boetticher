---
- name: Install Proxmox host prerequisites without upgrading the host
  ansible.builtin.apt:
    name:
      - git
      - rsync
      - openssh-client
      - openssh-server
      - python3
      - python3-apt
      - python3-debian
      - sudo
    state: present
    update_cache: false
    lock_timeout: 300
