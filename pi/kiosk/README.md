# Retired kiosk Blinkt assets

The former Companion `boetticher-blinkt.service` and renderer were removed.
Blinkt is now owned by the Controller's `boetticher-status.service`; this
directory remains as an explicit empty boundary for the existing Companion
source archive.
