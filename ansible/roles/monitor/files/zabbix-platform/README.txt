These definitions are boetticher-owned source inputs for the Zabbix API
provisioning projection. Their stable boetticher/platform namespace is the
ownership boundary: convergence may create or update these objects, but must
not delete or overwrite user-managed Zabbix objects.

The live API import and dashboard/template qualification are performed after
the monitor guest is reachable. This directory is deliberately non-secret.
