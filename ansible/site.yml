---
# boetticher guest convergence entry point.
# Runtime credentials are intentionally not represented in this playbook.
- name: Converge managed Debian guests
  hosts: managed
  become: true
  gather_facts: true
  roles:
    - base
    - chrony
    - dns
    - monitor
    - role: portal
      when: inventory_hostname == 'lab-portal-01'
